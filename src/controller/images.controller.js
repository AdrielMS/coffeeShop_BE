const imagesModel = require("../model/images.model"); //import the products.model module
const { unlink } = require("node:fs");
const imagesController = {
  getDetail: (req, res) => {},
  add: (req, res) => {},
  update: (req, res) => {},
  remove: (req, res) => {},
};

module.exports = imagesController;
